[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["backend/tests"]
markers = [
    "slow: expensive full-instance experiments excluded from normal verification",
    "integration: exercises multiple components using repository data",
]
